/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  // Remove rewrites for static export - API calls will be direct to backend URL
};

module.exports = nextConfig; 